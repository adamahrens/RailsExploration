<h1><%= @company.name %></h1>

<div>
  <table>
    <thead>
      <tr>
        <td>
          Name
        </td>
        <td>
          Hours Worked
        </td>
      </tr>
    </thead>
    <tbody>
      <% @company.projects.each do |project| %>
        <tr>
          <td>
            <span><%= link_to "#{ project.name }", project_path(project) %></span>
          </td>
          <td>
            <span><%= pluralize(project.hours_worked, 'hour') %></span>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>