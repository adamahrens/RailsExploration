<h1>All Projects</h1>
<p>
  <%= @projects.inspect %>
</p>

<div>
  <ul>
    <% @projects.each do |project| %>
      <li>
        <%= project.name %>
        |
        <span><%= link_to 'Show', project_path(project) %></span>
      </li>
    <% end %>
  </ul>
</div>